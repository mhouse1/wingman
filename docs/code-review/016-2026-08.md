# Code Review 016 — CR-015 Dispositions After the Fixture Consolidation

| Status | Date       | Wingman Version |
|--------|------------|-----------------|
| Draft  | 2026-08-14 | 1.7.1           |

## Scope

Disposition pass over the [Review 015](015-2026-08.md) findings, prescribed by
[ADR 072](../adr/072-calibration-screenshot-consolidation.md) decision 3
("their disposition is recorded in the next review-cycle file, referencing
this ADR"). No new review was performed; each item below records what happened
to a CR-015 finding after the 2026-08-13/14 recapture campaign, the fixture
consolidation (ADR 071/072), and the soak-session fixes.

## Dispositions

| ID | Original finding | Disposition |
|----|------------------|-------------|
| CR-015-01 | Respawn crop recalibration broke ADR 037/044/045 gates (old-layout fixtures) | **Resolved.** Gate corpus recaptured on the new layout via the repaired `make p1` lane; `make rr-path1-gate` PASS and `make ocr` 3 passed (2026-08-13). The capture lane itself needed three fixes (offset pinning, same-tick trigger/state race, replay-paced deadline) — recorded in ADR 071's consequences with regression tests. |
| CR-015-02 | FR-006 forbids the shipped d12 manoeuvre-limit exit | **Resolved (2026-08-16).** FR-006's statement amended to admit both release paths — clearance or `max_manoeuvre_s`, whichever first, the latter explicitly valid with a detection still present — with the d12 rationale and the SAF-006 backstop relationship recorded. Done alongside ADR 070's Draft → Accepted flip (V1–V5 resolved); exports regenerated via `make reqs`. |
| CR-015-03 | RESPAWND Levenshtein negative vacuous (distractor outside recalibrated crop) | **Superseded by ADR 072 d3.** The variant set is retired without recapture — accepted coverage loss. Token-level rejection coverage remains in `test_respawn_text_matches`. Revisit condition: a game update that changes overlay *rendering*, not position. |
| CR-015-04 | No discolored-frame OCR coverage (RESPAWNC deleted) | **Superseded by ADR 072 d3.** Same retirement decision and revisit condition. The interim self-activating recapture slots were implemented 2026-08-14 and removed the same day when ADR 072 landed. |
| CR-015-05 | `calibrate.py` `_save_config` strips every config.yaml comment | **Open.** Unchanged; the missile_evade and recalibration annotations remain one `make calibrate` run away from deletion. Comment-preserving writer (ruamel.yaml) or crops-only rewrite still recommended. |
| CR-015-06 | Tracked `performance.json` truncated to an empty shell in the working tree | **Resolved (2026-08-14).** Subsequent full-suite runs regenerated the complete dataset (476 tests, v1.7.1) before the tree was committed; the committed history carries full data, not the shell. |
| CR-015-07 | `TEST_SCREENSHOT_C` dangling at deleted RESPAWNC.png | **Resolved (via ADR 072 d3).** The constant was first repurposed as a recapture placeholder, then deleted outright with the variant retirement; `tests/constants.py` now documents the accepted losses instead. |
| CR-015-08 | `eject_closed_loop` config lost its ADR 069 breadcrumb | **Resolved (2026-08-14).** Two-line `# ADR 069` pointer restored above the block in config.yaml. Remains fragile until CR-015-05 (comment-stripping calibrate) is fixed. |
| CR-015-09 | `test_respawn_detection_positive` duplicates `test_level1_static_screenshot` | **Open.** Both still run the same subprocess on the same P1_050 frame; consolidation to one owner still recommended. |
| CR-015-10 | Sibling crops never systematically audited after the UI update | **Largely resolved by outcome.** The gate-corpus recapture plus ADR 072's calibration-map consolidation re-anchored respawn, click_to, good_luck, PLAY, CANCEL, MINIMAP, HEALTH and both ammo crops to fresh frames; the 2026-08-13/14 soak (5h17m, 54 missions, 100% click-to) exercised the load-bearing set live. Remaining unaudited: rare-popup crops (event_refresh, UNLOCK_CLOSE, INVITED, …) — low exposure, no observed failures. |

## Notes

- Open items carried forward: **CR-015-05** (comment-stripping
  calibrate — `yaml.dump` still in place), **CR-015-09** (test duplication —
  both copies still present), and the CR-016-01 follow-up (mission-entry
  `_mission_cancel.clear()` swallow race). CR-015-02 (requirements
  divergence) was resolved 2026-08-16 — see its disposition row above.
- This cycle also accumulated two new observations during the 2026-08-14 soak,
  recorded in code comments rather than findings: manoeuvre-limit exit
  overshoot (6–8 s under load, GIL/X starvation, benign) and XTest release
  latency under load (addressed by the scaled release-grace fix in
  `controller.py`).
- **CR-016-01 (new, Resolved): stuck-key leak from the test suite.** After a
  `make test` run the X server was left auto-repeating `i` into the focused
  window. Chain: tests neutralize `keyboard_module` via monkeypatch, which
  reverts at *test* teardown while unjoined mission daemon threads survive;
  the teardown's single `cancel_mission()` can be silently swallowed by
  mission entry's `_mission_cancel.clear()` (spawn-to-entry race); the
  surviving `mission_j20` thread then presses the REAL `i` (nose_up, 2 s
  hold), and pytest exiting mid-hold latches it server-side. Fixed with a
  session-end release-all-injectable-keys safety net in `conftest.py` plus
  drain-and-assert teardowns in `test_controller_no_keyboard.py` and
  `test_mission_cancel.py`. **Open follow-up:** the cancel-swallow race is a
  product bug too — `mission_j20`/`mission_loiter` entry clearing
  `_mission_cancel` erases any cancel that raced the thread spawn; moving the
  clear to the mission *starter* (before spawn) would close it for all
  callers.

## Legacy dispositions (verified 2026-08-14)

Older cycles carried items never formally closed. Verified against the current
tree and recorded here per the closed-file rule (the originals are unmodified):

| Original ID | Finding | Disposition |
|-------------|---------|-------------|
| CR-002 item 3.1 | No production instrumentation (OCR latency, event counts) | **Resolved** — long since implemented: `PerformanceTracker` (ADR 031/043: per-crop OCR timings, reaction latency, session JSONs, regression gate) and `MissionStatsTracker` (ADR 055: per-mission events, engagement survival). |
| CR-002 item 5.1 | No end-to-end test for the unattended mode cycle | **Resolved** — the ADR 044 runtime replay gate (`make rr-path1-gate`, in `make tp`) runs the real `wingman.main` loop through a full lobby→battle→respawn→end cycle with FSM assertions; the ADR 037 real-OCR lanes cover the same path with live OCR. |
| CR-002 item 5.2 | No end-to-end test for the game-starting loop event-refresh path | **Deferred (still).** The game-starting loop itself is now covered (PATH1 real-OCR lane: WAITING→STARTING→good_luck→BATTLE), but the event-refresh popup branch the item names remains untested — the popup is not deterministically reachable in a replay lane (same reachability class as ADR 072's retained root popup screenshots). |
| CR-008 H-4 | J20 hotkey bypasses the FSM (direct state write) | **Resolved** — the hotkey now routes through `analyzer.trigger_event("manual_force_battle")` (ADR 025/060 migration); no direct `analyzer.state =` writes remain in controller hotkey paths. |
| CR-008 H-4b | `_trigger()` holds the state lock during a 2 s join | **Resolved** — `_trigger` now explicitly defers all external side effects (mission cancel, starting-loop kickoff) until after `_state_lock` is released. |

## Session verification note (2026-08-14 05:09)

First live session with the CR-016-01 fix committed (`2b7769f`): 1h11m,
12 missions, 100% click-to, 0 errors. All 13 manual takeovers were genuine
human input (mixed keys, no same-key wingman release preceding any of them);
5 wingman echoes were correctly suppressed by the scaled grace window. The
delayed-echo false-takeover class is verified closed in production.
