# Code Review 015 — ADR 070 Body of Work + Respawn Recalibration

| Status | Date       | Wingman Version |
|--------|------------|-----------------|
| Draft  | 2026-08-13 | 1.7.1           |

## Scope

Deep review (xhigh, 10 finder angles + adversarial verification) of the
uncommitted working tree: the full ADR 070 MISSILE_EVADE_MODE implementation
(d1–d13 across `controller.py`, `behavior_tree.py`, `tick_handlers.py`,
`mission_stats.py`, `main.py`), the startup-stall watchdog change, the `;`
keysym fix, the per-engagement V5 metric, the respawn crop recalibration and
its test fallout, and the config/requirements/ADR edits.

Verified clean (no findings): the missile_evade config plumbing (all six keys
read under matching names with matching defaults, `enabled` gated in the BT
handler), the d4/d8/d11/d12 threading and key-release logic, the YAML reflow
(byte-identical key/value sets vs HEAD in `mission` and `eject_closed_loop`),
the replaced RESPAWN.png (dimension-identical, all consumers detect at 100%),
and the stall-watchdog host-shutdown removal (rationale preserved at its new
home in `main.py`).

## Findings

| ID | Sev | File | Finding | Disposition |
|----|-----|------|---------|-------------|
| CR-015-01 | High | `wingman/config.yaml:12` | Respawn crop recalibration breaks the ADR 037/044/045 gates: archived old-layout fixtures (`P1_050`, `P2_040`) OCR blank under the new crop (verified by extraction ×3 and an `analyzer_cli` run at confidence 0), so `make tp` / `tp-full` / `ocr` / `rr-live-path1-gate` fail their `respawn_detected` assertions. `make test` alone stays green — which is how it shipped. Fixtures need recapture on the new layout. | Open |
| CR-015-02 | High | `docs/requirements/002-functional.sdoc:114` | FR-006 forbids the shipped d12 exit: "release all three only after no fresh incoming detection…" with SAF-006 excepting only `max_hold_s`, while `max_manoeuvre_s` (the modal exit — 13/22 evades on 2026-08-13) releases with incoming present. Amend FR-006 (Draft) to admit the manoeuvre-limit exit, ref ADR 070 d12. | Open |
| CR-015-03 | Med | `tests/test_automated_levels.py:130` | RESPAWND Levenshtein negative is vacuous: the "natethegreat" distractor sits outside the new crop, so the test passes on empty terrain, not fuzzy-match rejection. Twin case in `test_analyzer.py`. Recapture RESPAWND on the new layout. | Open |
| CR-015-04 | Med | `tests/test_analyzer.py:91` | Zero discolored-frame OCR coverage remains (RESPAWNC deleted, no replacement) — the ADR 021 preprocessing pipeline is unguarded. Use the repo's `pytest.mark.skipif(not FRAME.exists(), …)` idiom (10+ uses in `test_minimap_bearing.py`) so the gap is visible and self-healing on recapture. | Open |
| CR-015-05 | Med | `tests/calibrate.py:57` | `_save_config` round-trips config.yaml through `yaml.dump`, stripping every comment — demonstrated in this diff (ADR 069 rationale block wiped by the add-crops run); the new missile_evade annotations die on the next `make calibrate`. Fix: comment-preserving writer (ruamel.yaml) or rewrite only `crops:`. | Open |
| CR-015-06 | Med | `tests/test-output/performance.json:1` | Tracked perf-history file currently truncated to an 84-byte empty shell (−219 lines vs HEAD); `performance_tracking.py` mines its git history, so committing injects a null v1.7.1 data point. `git checkout -- tests/test-output/performance.json` before any commit. | Open |
| CR-015-07 | Low | `tests/constants.py:9` | `TEST_SCREENSHOT_C` dangles at the deleted RESPAWNC.png (flagged by 7/10 finders). Delete or annotate; also the natural single home for the recapture note duplicated verbatim in two test files. | Open |
| CR-015-08 | Low | `wingman/config.yaml:199` | `eject_closed_loop` lost its only ADR 069 breadcrumb in the comment-strip collateral. Restore a one-line `# ADR 069` pointer (fragile until CR-015-05 is fixed). | Open |
| CR-015-09 | Low | `tests/test_automated_levels.py:96` | `test_respawn_detection_positive` is now a strict subset of `test_level1_static_screenshot` (same subprocess, same image, same assertion) and the positive-case matrix is maintained in parallel with `test_analyzer.py`. Consolidate to one owner. | Open |
| CR-015-10 | Low | `wingman/config.yaml:10` | Second reactive single-crop fix for one game update; sibling crops (HEALTH, ALTITUDE_SPEED, MINIMAP, click_to, good_luck, PLAY…) never systematically re-verified. Softened by 75 min of live evidence exercising the load-bearing ones — but that is incidental, not an audit. Do one calibration_map pass. | Open |

## Verification notes

- CR-015-01 was confirmed by four independent finders using three methods:
  fractional-crop extraction from the archived fixtures (blank under new
  coords, "RESPAWN" under old), and a live `analyzer_cli.py` run on `P1_050`
  returning "Respawn screen NOT detected" at confidence 0 under the current
  config.
- CR-015-02 verified against the `.sdoc` statement text directly.
- CR-015-03 verified by extracting the new crop from RESPAWND.png (terrain
  and a green marker only).
- CR-015-06 verified via `git diff --stat` (−219 lines) and the Makefile's
  documented force-add-at-release workflow.
- CR-015-10 is the sole PLAUSIBLE finding; all others are CONFIRMED.

## Recommended order of work

1. CR-015-06 (one command, protects history) → CR-015-02 (one `.sdoc`
   paragraph + `make reqs`) → CR-015-07/08 (trivial).
2. CR-015-01/03/04 share one root cause and one remedy: a recapture session
   on the new overlay layout (gate fixtures P1_050/P2_040/P2_050, RESPAWND,
   and a discolored RESPAWNC replacement), followed by `make tp-full`.
3. CR-015-05 decides whether any config comment survives long-term; do it
   before investing further in config annotations. CR-015-09/10 at leisure.
