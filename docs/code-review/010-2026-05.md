# Code Review 010 — Phase 3 Behavior Tree Triage

| Status   | Date       | Wingman Version |
|----------|------------|-----------------|
| Active   | 2026-05-23 | 1.6.8           |

## Scope

Triage of outstanding code-review items ahead of Phase 3 behavior-tree work.

This review does not restate all historical findings. It classifies the remaining
items into four buckets:

- Fix before Phase 3
- Fold into Phase 3
- Close as stale

Per `CLAUDE.md`, this file is the authoritative source for the current disposition
of the items listed below.

## Decision Summary

| Item | Source | Decision | Why | Exit Criterion |
|------|--------|----------|-----|----------------|
| J20 hotkey writes FSM state directly | CR-008 H-4 | Resolved Before Phase 3 (2026-05-23) | Hotkey now forces battle via FSM trigger path instead of direct state write | No direct assignment to analyzer FSM state on J20 hotkey path; trigger-driven transition in place |
| `_trigger()` holds `_state_lock` across side effects | CR-008 H-4b | Resolved Before Phase 3 (2026-05-23) | FSM trigger now defers expensive side effects until after lock release | State transition remains protected; cancel/start-loop callbacks execute outside `_state_lock` |
| Missing integration coverage for unattended lifecycle | CR-002 5.1/5.2, CR-003-11 | Resolved Before Phase 3 (2026-05-23) | Added FSM lifecycle regressions for unattended flow and GAME_STARTING loop callback behavior | Tests exercise GAME_LOBBY -> GAME_WAITING -> GAME_LOBBY and GAME_STARTING timeout/recovery transitions |
| Maneuver-key cancel regression-test gap | CR-003-12 | Resolved Before Phase 3 (2026-05-23) | Added focused tests for maneuver-key cancel/manual-takeover behavior and injected-key suppression | Unit tests cover BATTLE vs non-BATTLE outcomes and injected event ignore path |
| Duplicate `cancel_mission()` on GAME_LOBBY entry | CR-006 DESIGN-1 | Fold Into Phase 3 | Real but low-risk; noisy rather than incorrect, and likely touched naturally while refactoring lobby flow | Single authoritative lobby-entry cancel path remains |
| `GAME_STARTING` timeout ownership lives in controller | CR-006 DESIGN-3 | Fold Into Phase 3 | Design cleanup, not a blocker; current timeout is functional and may move naturally when Phase 3 reworks orchestration | Timeout ownership explicitly documented or moved during Phase 3 implementation |
| Click-to scan state-skip analysis | CR-006 DESIGN-2 | Fold Into Phase 3 | Low urgency relative to FSM correctness and lock-scope issues; revisit if BT changes click-to handling | Click-to polling states are explicitly reviewed during Phase 3 integration |
| Observability counters beyond timing histograms | CR-002 3.1 | Fold Into Phase 3 | `PerformanceTracker` covers OCR timing and reaction latency, but CR-002 also called out event counters (respawn/incoming/restart/click-to) that remain untracked in aggregated form | Phase 3 either implements the missing counters or records an explicit deferred rationale in the next review cycle |
| INCOMING crop bleed investigation | CR-009 R1 | Carry as Investigation | Real observation, but not a clear pre-BT blocker; current logic rejects the stray OCR correctly | Either screenshot-backed crop update or explicit suppression-rule decision |
| Health ceiling ratchet tuning | CR-009 R2 | Carry as Investigation | Relevant to future EVADE/health-driven tactics, but needs live-session evidence rather than preemptive tuning | Review 2-3 live sessions after Phase 3 health-driven tactics are enabled |

## Fix Before Phase 3

Status update (2026-05-23): all items in this section are implemented and covered by targeted regression tests. The section is retained as historical record of what was gated before Phase 3.

### 1. FSM bypass on J20 hotkey

**Source:** CR-008 H-4

Prior to the fix, code wrote `analyzer.state = GameState.GAME_BATTLE.name` directly
on the J20 hotkey path and then called `on_enter_GAME_BATTLE()` manually. That
bypassed the FSM as the single source of truth.

**Why it matters before BT:** ADR 024 assumes `game_state` is authoritative for tree
selection. A controller-side state bypass creates exactly the kind of implicit edge
that will be hard to reason about once tactics are selected by the BT.

**Required outcome:** Replace the direct state write with a trigger-driven FSM path.

**Implementation evidence:**

- `wingman/controller.py` hotkey path now calls analyzer trigger (`manual_force_battle`) instead of direct state assignment.
- `wingman/analyzer.py` includes `manual_force_battle` transition in `_FSM_TRANSITIONS`.

### 2. `_trigger()` lock scope

**Source:** CR-008 H-4b

`GameStateAnalyzer._trigger()` currently holds `_state_lock` while entry hooks can
call external side effects, including mission-cancel paths that join threads.

**Why it matters before BT:** Phase 3 increases the importance of responsive and
predictable state transitions. Long critical sections in the FSM layer are not
eliminated by adopting a BT.

**Required outcome:** Keep state mutation protected, but move expensive side effects
outside the lock.

**Implementation evidence:**

- `wingman/analyzer.py::_trigger()` now collects post-transition callbacks while locked and executes them after `_state_lock` is released.
- `on_enter_GAME_LOBBY`, `on_enter_GAME_STARTING`, and `on_enter_GAME_BATTLE_MANUAL` no longer invoke expensive side effects directly.

### 3. FSM-level lifecycle tests

**Source:** CR-002 5.1/5.2 and CR-003-11

The old rationale for deferring some tests was that the boolean-flag surface was in
flight. ADR 025 is already implemented. The current missing coverage should now be
written against the FSM surface rather than the deleted flag model.

**Required outcome:** Add regression coverage for the unattended lifecycle and the
GAME_STARTING loop against the current `game_state` / `_trigger()` model.

**Implementation evidence:**

- `tests/test_analyzer_lifecycle.py::test_fsm_unattended_lifecycle_and_starting_loop_callbacks`
- `tests/test_analyzer_lifecycle.py::test_trigger_runs_side_effect_callbacks_outside_state_lock`

### 4. Maneuver-key cancel regression test

**Source:** CR-003-12

The maneuver-key path remains active in `Controller` and still drives mission
cancellation / manual takeover behavior. This path should have a focused regression
test independent of unattended lifecycle coverage.

**Required outcome:** Add a regression test for maneuver-key behavior across at
least `GAME_BATTLE` and non-battle states.

**Implementation evidence:**

- `tests/test_controller_no_keyboard.py::test_maneuver_key_triggers_manual_takeover_in_battle`
- `tests/test_controller_no_keyboard.py::test_maneuver_key_does_not_trigger_manual_takeover_outside_battle`
- `tests/test_controller_no_keyboard.py::test_maneuver_key_ignores_injected_events`

## Fold Into Phase 3

### 1. Duplicate lobby-entry cancellation

Keep this as opportunistic cleanup. It is visible and unnecessary, but not a Phase 3
gate because the duplicate call is idempotent and has not shown correctness fallout.

### 2. Timeout ownership for `GAME_STARTING`

This should be revisited while Phase 3 decides where orchestration belongs, but the
current controller-owned timeout is functional and not worth a pre-BT detour.

### 3. Click-to polling state review

Re-evaluate once Phase 3 integration decides whether click-through remains outside
the BT or becomes part of a more explicit flow wrapper.

### 4. Observability counters from CR-002

`PerformanceTracker` already provides OCR timing and reaction-latency tracking, but
CR-002 also called out aggregate event counters (respawn, incoming, mission restart,
click-to) that remain partially uncovered. Treat this as carry-forward observability
work, not as stale debt.

## Carry As Investigation

### 1. INCOMING crop bleed

Keep as an investigation item, not a pre-Phase-3 fix. The system is currently
rejecting the observed stray OCR correctly, and crop movement without screenshot
evidence is higher risk than the current false-negative-free behavior.

### 2. Health ceiling ratchet tuning

Keep as a live-data calibration item. It becomes more important when Phase 3 starts
using health thresholds for tactics such as EVADE, but current evidence supports more
observation before tuning filter constants.

## References

- CR-002: `docs/code-review/002-2026-03.md`
- CR-003: `docs/code-review/003-2026-04.md`
- CR-006: `docs/code-review/006-2026-05.md`
- CR-008: `docs/code-review/008-2026-05.md`
- CR-009: `docs/code-review/009-2026-05.md`
- ADR 024: `docs/adr/024-phase3-behavior-tree-architecture.md`
- ADR 025: `docs/adr/025-formalise-game-state-machine.md`